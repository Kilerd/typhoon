# Constructor arguments are keywords, so a field can only be filled once
# (DESIGN 3.8).
# milestone: M2
# error: field `x` specified twice

class Point:
    x: float
    y: float

fn main():
    p = Point(x=1.0, x=2.0, y=3.0)
    print(p.x)
