# Writing through an index is checked against the element type (DESIGN 4.1).
# milestone: M2
# error: mismatched types: expected `int`, found `str`

fn main():
    xs: list<int> = [1, 2]
    xs[0] = "a"
    print(xs[0])
