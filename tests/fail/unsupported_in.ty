# `in` on a `str` is a substring test, so both sides must be `str`.
# milestone: M2
# error: `in` on a `str` tests for a substring, found `int`

fn main():
    s = "typhoon"
    if 1 in s:
        print("yes")
