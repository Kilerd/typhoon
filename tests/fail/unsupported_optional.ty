# M2 allows `C | None` for a class `C`; every other union waits for M3
# (DESIGN 3.8, 3.10).
# milestone: M2
# error: an optional type (`int | None`) is not supported yet (planned for M3)

fn find(n: int) -> int | None:
    return n

fn main():
    print(1)
