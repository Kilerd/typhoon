# `str(x)` joins the language with the rest of the string API in M2; an
# f-string does the same job today.
# milestone: M1
# error: the builtin `str` is not supported yet (planned for M2)

fn main():
    print(str(1))
