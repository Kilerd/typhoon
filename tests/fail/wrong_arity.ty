# Calls are checked against the declared signature.
# milestone: M1
# error: takes 2 arguments but 3 were supplied

fn add(a: int, b: int) -> int:
    return a + b

fn main():
    print(add(1, 2, 3))
