# `len` is defined on `str`, `list<T>` and `tuple<...>` only (DESIGN 4.6).
# milestone: M2
# error: `len` is not defined for `Point`

class Point:
    x: float

fn main():
    print(len(Point(x=1.0)))
