# Keyword arguments are matched to parameter names (DESIGN 3.2).
# milestone: M1
# error: has no parameter named `c`

fn add(a: int, b: int) -> int:
    return a + b

fn main():
    print(add(a=1, c=2))
