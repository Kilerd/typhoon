# Inference is local, so an empty container literal needs an annotation
# (DESIGN 4.4).
# milestone: M2
# error: cannot infer the element type of an empty list

fn main():
    xs = []
    print(len(xs))
