# The container methods have fixed signatures, checked like any call
# (DESIGN 4.6).
# milestone: M2
# error: `list<int>.append` takes 1 argument but 0 were supplied

fn main():
    xs: list<int> = [1]
    xs.append()
