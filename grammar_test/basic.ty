struct A {
        inner: i32,
    }
    struct B {
        inner: A,
    }
    fn main() -> i32 {
        let a: A = A {inner: 4};
        let b: B = B {inner: a};
        return b.inner.inner;
    }